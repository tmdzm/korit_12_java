# 싱글톤

## 정의
- 소프트웨어 '디자인 패턴'중 하나<br>
어떤 클래스가 '하나'의 인스턴스만 갖도록 보장하고 , 그 인스턴스에 전역적인 접근법을 제공하는 패턴
예를 들어, 데이터베이스 연결과 같은 리소스는 프로그램 전체에서 하나의 인스턴스만 사용해야 할때가 많기 때문에
싱글톤 패턴을 사용함

## 특징
1. 하나의 인스턴스만 존재 : 하나의 인스턴스만 생성되고, 이 인스턴스는 프로그램이 실행되는 동안 유지
2. 전역 접근법 : 싱글톤 인스턴스는 정적 메서드를 통하여 어디서든 접근 가능 -> 객체명.메서드명();
x-> 클래스명.메서드명();
3. 인스턴스의 생명 주기 관리

## 장점
1. 메모리 절약 : 불필요한 인스턴스 생성을 방지하여 메모리 절약가능 (변수/ 메서드의 장점과 같다.)
2. 글로벌(전역) 접근 : 메모리 관리가 용이 , 다만 잦은 접근
3. 일관성 유지 : 어플리케이션 전반에서 하나의 인스턴스만 사용, 상태 일관성 유지

## 단점
1. 테스트 어려움 : 싱글톤 패턴은 전역적으로 사용되므로 '단위 테스트'가 어려울 수 있다.
2. 의존성 숨김 : 싱글톤을 남용하면 클래스 간의 의존성을 추정하기 어려울 수 있음

# 제네릭

## 개념 및 정의
- 클래스나 메서드가 다룰 데이터 타입을 미리 명시하여 재사용성과 유연선을 높이는 기술
- 정의 단계가 아니라, 있는것 사용할때 커스텀

### 타입 매개변수
- 클래스나 메서드에서 사용할 실제 타입을 '대신하여 사용하는 매개변수'
- 일반적으로 알파벳 대문자 하나로 표기함.
    1. T : 일반적으로 클래스 타입을 나타낼때 사용
    2. E(element) : 컬렉션의 요소를 나타낼때 사용
    3. K(key) : Map의 key를 나타낼때 사용
    4. V(value) : Map의 value 또는 제네릭 타입의 리턴값으로 나타낼때 사용

### 재사용성
- 제네릭을 사용하면 다야한 자료형에 대해 하나의 클래스나 메서드를 정의할 수 있어 코드의 재사용성을 높이고 중복을 줄인다.

ch18_generic 밑에 bmi 패키지를 생성하시오.

Person 클래스를 정의하고
field를 String name/ double height / double weight로 작성
PersonMain 클래스를 생성하시오.
Bmi에서 method로  calcBmi(Person person); 형태로 정의하시오 -> return type double

printResult(double bmi)를 적용, 조건문을 사용하여
- 김일 님의 키는 xxxcm, 몸무게는 xxxkg, bmi 지수는 xxx으로 저체중/정상/비만전단계/1단꼐비만/2단계비만/3단계비만

PersonMain에서 Person 객체 생성, Bmi 객체를 생성한 후 calcBmi()및 printResult

```java
public static void main(String[] args) {
        Person person = new Person("김일",168,68);
        Bmi bmi = new Bmi();

        bmi.printResult(person);

        //bmi.check(person);

        //Dog dog1 = new Dog("강아지",69,32); dog를 따로 쓰는게 아니면 에러난다.    
    }
```

이런 상황을 대비해서 제네릭을 쓴다.
